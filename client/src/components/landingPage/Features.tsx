import styles from "../../utils/styles";

export default function Features() {
  return (
    <div id="introduction" className={`bg-primary ${styles.flexStart} ${styles.paddingX}`}>
        <div className={`${styles.boxWidth}`}>
            Introduction
        </div>
    </div>
  )
}
